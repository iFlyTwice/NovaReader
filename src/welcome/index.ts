// Welcome page is now self-contained in the HTML file
// This script adds any additional functionality needed

console.log('Welcome page initialized');

// No need to explicitly create the landing page since it's included directly in the HTML
